import data from '../../content/hotel-info.json'

export const checkinData = {
  wifi: data.wifi,
  checkinTime: data.checkinTime,
  checkoutTime: data.checkoutTime,
  curfew: data.curfew,
  contact: data.contact,
  rules: data.rules,
}
